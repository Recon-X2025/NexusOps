# GAP_ANALYSIS.md — Living Gap Tracker

**Authoritative, living gap tracker.** Supersedes the dated 2026-07-03 platform-gap set and
2026-06-30 competitive analysis (now in `docs/archive/`). Update this file in place as items ship.

- **Last verified:** 2026-07-20 (read-only, `file:line`-grounded code audit)
- **Migration head:** `0052_odd_forgotten_wall` (G1–G17 gap-closure run: migs `0041`–`0052`)
- **Method:** verified against actual source, not prior gap docs (many earlier "gaps" have since shipped).
- **Legend:** ✅ REAL (shipped, wired, tested) · 🟡 PARTIAL · 🔴 STUB (schema only, no logic) · ⛔ MISSING

> How to use: when you close an item, flip its status, add the shipping `file:line` + test, and
> move it to the "Recently shipped" log at the bottom. Keep the open table honest.

---

## 1. Shipped & production-grade (verified REAL)

These were flagged as gaps in older audits but are now confirmed real in code. Do **not** re-open
without re-reading the cited source.

| Area | Evidence (`file:line`) |
|------|------------------------|
| GST engine (CGST/SGST/IGST, ITC per §49, RCM, filing calendar) | `packages/payroll-math/src/gst-engine.ts:53` |
| GSTR-1 dynamic per-line rate (18% hardcode removed) | `apps/api/src/routers/accounting.ts:731`; test `gstr1-rate-grouping.test.ts` |
| GSTR-2A/2B reconciliation (tolerance line-match) | `packages/payroll-math/src/gst-engine.ts:246` |
| E-invoicing IRN/QR (ClearTax adapter, BullMQ) | `apps/api/src/workflows/irnGenerationWorkflow.ts` |
| Balance sheet (Assets = Liab + Equity, balances) | `apps/api/src/routers/accounting.ts:591`; test `balance-sheet-pnl.test.ts` |
| Depreciation engine (SLM/WDV, Schedule II) | `packages/payroll-math/src/depreciation.ts:127` |
| Gratuity accrual (Payment of Gratuity Act) | `packages/payroll-math/src/gratuity.ts:82` |
| Leave accrual / carry-forward / encashment | `packages/payroll-math/src/leave-accrual.ts:38` |
| PF / ESI / PT / LWF statutory computation | `packages/payroll-math/src/statutory-deductions.ts:74` |
| TDS / income tax (Finance Act 2025 slabs, dual-regime) | `packages/payroll-math/src/tax-engine.ts:88` |
| 3-way match + journal debit=credit invariant | `apps/api/src/lib/invoice-po-match.ts:161`; `accounting.ts:282` |
| Real accrual accounts (no placeholder UUIDs) | test `po-accrual-accounts.test.ts` |
| DPDP consent / DSR / breach automation + sweeps | `apps/api/src/lib/dpdp-sweeps.ts`; Temporal `dpdpSweepWorkflow` |
| DPDP retention floor (8y) + peppered PII hashing | `apps/api/src/lib/retention.ts`; `pii-hash.ts` |
| Tamper-evident audit hash chain (`verifyAuditChain`) | `packages/db/src/schema/auth.ts:300`; `lib/audit-hash.ts` |
| Vuln CVSS→SLA + escalation loop | `apps/api/src/workflows/vulnerabilitySlaWorkflow.ts` |
| MFA enrollments (TOTP + bcrypt backup codes) | `packages/db/src/schema/auth.ts:144` |
| ITSM: event correlation, on-call escalation, SLA re-arm, deploy→MTTR | `services/itom-correlation.ts`; `workflows/escalationWorkflow.ts`; `deploy-mttr.test.ts` |
| Workflow trigger layer (scheduled + event) | `apps/api/src/workflows/workflowTriggerWorkflow.ts` |
| Outbound webhook dispatcher (HMAC-signed, at-least-once) | `apps/api/src/workflows/webhookDispatchWorkflow.ts` |
| Super-admin / MAC monitoring (JWT-gated) | `apps/api/src/http/super-admin.ts` |
| OKR cascade hierarchy | `apps/api/src/routers/hr.ts`; test `okr-cascade.test.ts` |

---

## 2. Open gaps (India go-live priority order)

### Regulatory / statutory (highest risk)

| # | Item | Status | Evidence | Pending work | Owner | Target |
|---|------|--------|----------|--------------|-------|--------|
| G1 | Labour Codes Nov-2025 wage-base + ceilings | ✅ | Versioned ceilings resolver (`lib/india/statutory-ceilings.ts`, `statutory_ceilings` config) drives EPF/ESI/PT/LWF as data. Labour Codes 2025 shipped: `calculateLabourCodeWageBase` applies the Code on Wages s.2(y) **50%-inclusion proviso** (excess of exclusions over 50% of total remuneration clawed back into wages) BEFORE the PF/ESI clamp; `computeStatutoryBonusEligibility` gates statutory bonus on the Payment of Bonus Act ceiling. Wired into `computeEmployeePayslip` (proviso + bonus gate applied ONLY when a `bonus_eligibility_ceiling` is resolved, so pre-Labour-Code orgs are unchanged) + resolver `bonusEligibilityCeiling` override. Migration `0054` (self-contained enum-swap for `bonus_eligibility_ceiling` + coalesced-scope unique arbiter + platform-default seed pf ₹15k / esi ₹21k / bonus ₹21k @ 2025-11-21, idempotent `ON CONFLICT`) | Shipped — 14 tests (pure proviso/eligibility, payslip clamp @ ₹15k + visible sub-ceiling add-back, bonus gate, Dec-2025 effective-date resolver); migration validated on a throwaway DB | _shipped_ | 2026-07-21 |
| G2 | Statutory filing returns (ECR / ESI / PT challan) | ✅ | All three compute→file loops shipped on the same template. **EPFO ECR:** `services/integrations/epfo-ecr.ts` + `workflows/statutoryFilingWorkflow.ts` + `india-compliance.filing.{submit,status}`, `epfo_ecr_submissions.portalError/lastAttemptAt` + status enum (mig `0049`). **ESIC MC return:** `services/integrations/esic-return.ts` GSP adapter (17-digit employer-code validated, registered) + `workflows/esiReturnWorkflow.ts` (BullMQ `coheronconnect-esi-return`, 5-attempt backoff, `not_configured` soft-fail, challan persist, idempotent) + `india-compliance.filing.{submitEsi,statusEsi}` rebuilds the MC member-lines from the payroll run at statutory rates (0.75%/3.25%). **State PT challan:** `services/integrations/pt-challan.ts` GSP adapter (per-state routing, registered) + `workflows/ptChallanWorkflow.ts` (`coheronconnect-pt-challan`, same shape) + `india-compliance.filing.{submitPt,statusPt}` rebuilds the per-employee PT lines (`professionalTax > 0`). New `esi_challan_records` + `pt_challan_records` tables + `statutory_return_status` enum (mig `0055`); both workflows wired into `services/workflow.ts` (queue+worker+failed-handler+shutdown) | Shipped — 10 DB tests (`esi-return.test.ts` 5 + `pt-challan.test.ts` 5: push+persist, not_configured soft-fail, failed+rethrow, idempotent skip, tenancy); mig `0055` validated on a throwaway DB (0000→0055 clean, zero drift) | _shipped_ | 2026-07-21 |
| G3 | MCA21 filing | ✅ | MCA21 V3 gateway push shipped: `services/integrations/mca21.ts` adapter (CIN-validated, registered) + `workflows/mca21FilingWorkflow.ts` (BullMQ, 5-attempt backoff, `not_configured` soft-fail, SRN + ack persist, idempotent on SRN, malformed-payload guard) + `legal.mca21.{prepare,submit,status,list}` carrying the prepared `{formCode,period,formData}` e-Form body; `mca_filing_records` extended with `payload_json`/`ack_json`/`portal_error`/`last_attempt_at` + `srn` index (mig `0051`) | 10 DB tests (prepare/submit idempotency, file round-trip, SRN persist, not_configured, failed+retry, malformed-payload, tenancy) | _shipped_ | 2026-07-20 |
| G4 | E-Way Bill | ✅ | NIC portal push shipped: `services/integrations/nic-ewaybill.ts` GSP adapter (generate+cancel, registered) + `workflows/ewayBillWorkflow.ts` (BullMQ, 5-attempt backoff, `not_configured` soft-fail, EWB-no + validUpto persist, idempotent) + `financial.ewayBill.{generate,cancel,status,listForInvoice}` gated on `isEWayBillRequired` (₹50k goods), builds the NIC EWB-01 payload from the invoice; new `eway_bills` table + `eway_bill_status` enum (mig `0050`) | 9 DB tests (threshold gate, payload, generate/cancel round-trip, not_configured, failed+retry, idempotent, tenancy) | _shipped_ | 2026-07-20 |

### CRM intelligence (stored-but-unused)

| # | Item | Status | Evidence | Pending work | Owner | Target |
|---|------|--------|----------|--------------|-------|--------|
| G5 | Lead scoring | ✅ | `lib/crm/lead-score.ts` pure deterministic scorer (source/status/seniority/contactability) + `lib/crm/lead-scoring-rules.ts` versioned-config resolver (`lead_scoring_rules`, mig `0044`, org override → platform default → built-in); `lib/crm/lead-write.ts` persists score on every create/update (flat + nested) + rescore sweep; `crm.leadScoring.{effective,history,publish,rescore}` admin router publishes weights and rescores existing leads | Shipped — `desc(score)` list now meaningful; 6 DB tests | _shipped_ | 2026-07-20 |
| G6 | CPQ tax/GST on quotes | ✅ | `crmQuotes` gained `placeOfSupply/isInterstate/taxableValue/cgst/sgst/igst/taxTotal` + per-line `hsnCode/gstRate`; `crmAccounts` gained `stateCode/gstin` (mig `0045`); `lib/crm/quote-tax.ts` computes per-line GST (discount before tax, intra=CGST+SGST vs inter=IGST via org GSTIN state vs buyer account state) and rolls up; both `deals.quotes.create/update` + flat `createQuote/updateQuote` route through it and re-tax on line/discount edits | Shipped — 8 DB tests (intra/inter/discount/mixed-rate/default/re-tax) | _shipped_ | 2026-07-20 |
| G7 | Lead→deal lossless conversion | ✅ | `lib/crm/lead-convert.ts` upserts account+contact, carries both onto the deal, re-points open activities, idempotent; `crmLeads.accountId/contactId` added (mig `0043`); both `crm.convertLead` + `crm.leads.convert` route through it | Shipped — orphaned `routers/crm.ts` shadow deleted; 6 DB tests | _shipped_ | 2026-07-20 |

### People / Asset

| # | Item | Status | Evidence | Pending work | Owner | Target |
|---|------|--------|----------|--------------|-------|--------|
| G8 | Attendance capture → payroll LOP | ✅ | `attendance_records` (unique `orgId/employeeId/date`) + capture surface (`clockIn/clockOut/bulkMark`, now upserts so corrections stick); `lib/india/attendance-lop.ts` derives paid vs LOP days (absent→1.0, half_day→0.5), wired into payroll at `payroll-run-aggregates.ts` + `payroll.ts` so unpaid absence reduces gross. **Leave→attendance reflex** (`lib/india/leave-attendance.ts`): approving a leave writes `absent` (unpaid→LOP) / `on_leave` (paid→zero-LOP) rows across the range, upserted in the approve tx. **Employee self-service capture** (first-party HRMS): `attendance.{signIn,signOut,myToday}` — `protectedProcedure` resolving the employee from `ctx.user` (no `hr.write`, no foreign id); one row/day, first-in (upsert `coalesce(check_in, excluded.check_in)`) / last-out (`check_out=now` + `hours_worked`), refuses users with no linked employee. **External daily-attendance ingest** (`lib/india/attendance-ingest.ts` + `attendance.ingest`): a biometric/HRMS feed keyed by human `employeeCode` is normalised (derives status/hours/late/overtime, last-write-wins dedupe), resolved to UUID scoped to the org (unknown codes skipped+reported), idempotently upserted. **Shift-schedule-aware punch** (`shift_schedules` table + `employees.shiftScheduleId` FK, mig `0053`): `lib/india/shift-schedule.ts` `resolveShift` (assigned → org default → built-in 09:00/8h baseline) + pure `derivePunch` classifies a punch (present/late/half_day, lateMinutes/overtimeMinutes/hoursWorked, all clamped ≥0); `signIn`/`signOut` derive status against the effective shift; admin CRUD `hr.shifts.{list,create,update,setDefault,assign}` (setDefault/create demote the incumbent so the partial unique index holds one default per org; assign is org-scoped) | Shipped — 66 tests (14 shift-resolve unit, 11 shift-router e2e, 7 self-service e2e, 10 normalise unit, 5 ingest e2e, 8 reflex, 7 expand, 4 LOP) | _shipped_ | 2026-07-21 |
| G9 | SAM installed-vs-entitled reconciliation | ✅ | `softwareLicenses.installedCount/reconciledAt` (mig `0047`); `lib/sam/license-reconcile.ts` pure `reconcileLicense` (installed−entitled → over_deployed/under_utilized/at_parity/unknown + shortfall) + `ingestInstalledCount`/`reconcileOrgLicenses` (active-assignment count, audit-risk-first sort); `assets.licenses.{ingestInstalled,reconcile}` router | Shipped — 6 DB tests (over/under/parity/unknown, ordering, tenant isolation) | _shipped_ | 2026-07-20 |
| G10 | Asset expiry alerting | ✅ | `expiryAlertWorkflow` BullMQ sweep: contract renewal-notice window + asset warranty horizon → owner notification, idempotent via `notifications.sourceType/sourceId` | — | shipped (mig 0047) | 2026-07-20 |
| G11 | CMDB cycle detection | ✅ | `assets.cmdb.linkCi` runs a directed-cycle DFS from target (org-scoped) before insert; rejects self-loops + any edge closing a cycle | — | shipped | 2026-07-20 |
| G12 | OKR progress rollup | ✅ | `okrObjectives.rollupProgress` persisted column (mig `0046`); `lib/hr/okr-rollup.ts` recomputes an objective's own progress from its KRs then re-persists the whole org forest's rollup (own + all descendants' own, cycle-safe); wired into `createKeyResult`/`updateKeyResult` (KR change) + `setParent` (re-parent); `okr.cascade` now surfaces the persisted column instead of on-read traversal | Shipped — 5 OKR tests (multi-level persist, leaf→ancestor re-roll, on-read parity, re-parent) | _shipped_ | 2026-07-20 |

### Platform / infra (lower priority for Phase 1)

| # | Item | Status | Evidence | Pending work | Owner | Target |
|---|------|--------|----------|--------------|-------|--------|
| G13 | SMS delivery (transactional) | ✅ | `sms` is a first-class fan-out channel in `runNotificationDispatch`; DLT payload (template id + E.164 + vars) routes through the `sms_msg91` adapter, best-effort no-op when unconnected; `sendNotification` opts in via `payload.sms` | — | shipped | 2026-07-20 |
| G14 | RLS (row-level security) | ✅ | Hand-written mig `0052` provisions a non-privileged `app_runtime` role (NOLOGIN/NOSUPERUSER/NOBYPASSRLS + grants) and `ENABLE`+`FORCE ROW LEVEL SECURITY` with a `tenant_isolation` policy (`USING`/`WITH CHECK` on `org_id = current_setting('app.org_id')::uuid`, bypass-when-GUC-unset, `OR org_id IS NULL` for nullable-org tables) on all 191 tenant tables; `rlsTenant` middleware in `lib/trpc.ts` wraps every authed request in a tx that `SET LOCAL ROLE app_runtime` + `set_config('app.org_id', …, true)` and swaps `ctx.db` to the tx handle (both tx-local, no pool leak; superusers/seeds/workers/migrator stay unconstrained). **Key correctness catch:** the app DB user is a superuser+BYPASSRLS, so `FORCE` alone is silently bypassed — the role drop is what makes RLS enforce. | Shipped — 8 DB-level isolation tests (`rls-tenant-isolation.test.ts`): raw un-filtered SELECT sees only own org, cross-org INSERT/UPDATE/DELETE blocked, platform-default rows visible, no role leak to pool | _shipped_ | 2026-07-20 |
| G15 | KMS (key management) | ✅ | `services/kms.ts` pluggable `KmsProvider` (local APP_SECRET-derived KEK + real `AwsKmsProvider` via `@aws-sdk/client-kms` GenerateDataKey/Decrypt + DEK cache); `services/encryption.ts` envelope codec (`v2:` per-payload AES-256-GCM DEK, wrapped DEK stored inline) with `*Envelope` async read/write auto-detecting legacy CBC; all integration-config + MFA TOTP-seed sites migrated; boot guard `assertKmsConfigured()` in `index.ts` (refuses local-KMS in prod without opt-in) | Shipped — 18 kms-encryption + 3 integration-config-envelope + MFA envelope regression green; legacy CBC blobs still decrypt (no forced migration) | _shipped_ | 2026-07-20 |
| G16 | E-sign provider adapter | ✅ | `esign/docusign.ts` implements `EsignProvider` (envelope create + embedded recipient-view URL, status map, combined-PDF fetch+sha256, Connect HMAC-SHA256 callback verify); registered in `esign/index.ts` (`IMPLEMENTED_ESIGN_PROVIDERS` + `_assertRegistered`); provider-generic `handleEsignCallback` webhook at `/webhooks/esign/{emudhra,docusign}`; catalog entry in `integrations.ts` | Shipped — 8 adapter + 6 gating DB tests | _shipped_ | 2026-07-20 |
| G17 | RoPA full CRUD router | ✅ | `dpdpProcessingActivities` gained `status`/`retiredAt` (mig `0048`); `compliance.ropa.{list,get,create,update,signOff,retire}` — org-scoped §5 register with `status`/`lawfulBasis` filters, DPO sign-off gate, soft-retire (retired rows kept for audit, edits blocked) | Shipped — 6 DB tests (create/list/filter/update/sign-off/retire/tenancy) | _shipped_ | 2026-07-20 |

---

## 3. Recommended sequencing (India go-live)

1. ~~**G1** Labour Codes ceilings~~ — ✅ shipped (versioned ceilings + s.2(y) 50% wage-base proviso + bonus eligibility, mig `0054`).
2. ~~**G2** Statutory filing returns~~ — ✅ shipped (EPFO ECR + ESIC MC return + state PT challan portal loops, mig `0055`).
3. **G3 / G4** MCA21 + E-Way Bill — regulatory filing surfaces.
4. **G5–G7** CRM intelligence — revenue leverage once regulatory floor is set.
5. **G8–G12** People/Asset depth.
6. **G13–G17** Platform/infra hardening.

---

## 4. Recently shipped log

_Append entries here as gaps close (date · item · `file:line` · test)._

- 2026-07-20 · Baseline established from verified audit at head `0032_damp_la_nuit`.
- 2026-07-20 · G12 OKR rollup persisted · `lib/hr/okr-rollup.ts`, `okrObjectives.rollupProgress` (mig `0046`) · `okr-cascade.test.ts` (5 G12 cases).
- 2026-07-20 · G11 SAM reconciliation · `lib/sam/license-reconcile.ts`, `softwareLicenses.installedCount/reconciledAt` (mig `0047`) · `sam-reconcile.test.ts` (6 cases).
- 2026-07-20 · G9 expiry alerting · `workflows/expiryAlertWorkflow.ts` (contract renewal-notice + asset warranty sweeps), wired in `services/workflow.ts` · `expiry-alert-sweep.test.ts` (6 cases).
- 2026-07-20 · G10 CMDB cycle detection (already shipped) · `assets.cmdb.linkCi` directed-cycle DFS guard · `cmdb-cycle.test.ts` (5 cases).
- 2026-07-20 · G13 SMS channel · `sms` wired into `runNotificationDispatch` via `sms_msg91` adapter, `sendNotification` `payload.sms` opt-in · `sms-notification-channel.test.ts` (5 cases).
- 2026-07-20 · G16 RoPA router · `compliance.ropa` CRUD + DPO sign-off + soft-retire, `dpdpProcessingActivities.status/retiredAt` (mig `0048`) · `ropa-router.test.ts` (6 cases).
- 2026-07-20 · G2 EPFO ECR portal push · `services/integrations/epfo-ecr.ts` GSP adapter + `workflows/statutoryFilingWorkflow.ts` + `india-compliance.filing.{submit,status}`, `epfo_ecr_submissions.portalError/lastAttemptAt` + status enum (mig `0049`) · `statutory-filing.test.ts` (5 cases).
- 2026-07-20 · G4 E-Way Bill NIC push · `services/integrations/nic-ewaybill.ts` GSP adapter (generate+cancel) + `workflows/ewayBillWorkflow.ts` + `financial.ewayBill.{generate,cancel,status,listForInvoice}` gated on `isEWayBillRequired` (₹50k goods), new `eway_bills` table + `eway_bill_status` enum (mig `0050`) · `eway-bill.test.ts` (9 cases).
- 2026-07-20 · G3 MCA21 V3 filing · `services/integrations/mca21.ts` gateway adapter (CIN-validated) + `workflows/mca21FilingWorkflow.ts` (SRN idempotent, `not_configured` soft-fail, malformed-payload guard) + `legal.mca21.{prepare,submit,status,list}`, `mca_filing_records` extended with `payload_json/ack_json/portal_error/last_attempt_at` + `srn` index (mig `0051`) · `mca21-filing.test.ts` (10 cases).
- 2026-07-20 · G16 DocuSign e-sign adapter · `services/esign/docusign.ts` (`EsignProvider`: envelope create + embedded recipient-view URL, envelope/recipient status map, combined-PDF fetch+sha256, Connect HMAC-SHA256 callback verify) + registered in `services/esign/index.ts` + provider-generic `handleEsignCallback` webhook (`/webhooks/esign/{emudhra,docusign}`) + `integrations.ts` catalog entry; no migration (`docusign` already in `esign_provider` enum) · `docusign-adapter.test.ts` (8 cases) + `esign-provider-gating.test.ts` (6 cases).
- 2026-07-20 · G15 KMS-backed envelope encryption · `services/kms.ts` pluggable `KmsProvider` (local APP_SECRET-derived KEK + `AwsKmsProvider` via `@aws-sdk/client-kms` GenerateDataKey/Decrypt + copy-safe DEK cache) + `services/encryption.ts` `v2:` envelope codec (per-payload AES-256-GCM DEK, wrapped DEK stored inline) with async `*Envelope` read/write auto-detecting legacy CBC; migrated all integration-config decrypt sites (registry/sap/jira/webhooks/esign + 6 workflows) + `integrations.upsertIntegration` write path + MFA TOTP-seed sites in `auth.ts`; boot guard `assertKmsConfigured()` in `index.ts`; no migration (`integrations.kmsKeyId/dekWrappedB64` already present) · `kms-encryption.test.ts` (18 cases) + `integration-config-envelope.test.ts` (3 cases) + MFA envelope regression.
- 2026-07-20 · G14 Postgres RLS (defence-in-depth tenant isolation) · hand-written mig `0052_odd_forgotten_wall` provisions a non-privileged `app_runtime` role (NOLOGIN/NOSUPERUSER/NOBYPASSRLS + schema/table/sequence grants + default privileges) and `ENABLE`+`FORCE ROW LEVEL SECURITY` with a `tenant_isolation` policy (`org_id = current_setting('app.org_id')::uuid`, bypass-when-GUC-unset, `OR org_id IS NULL` for nullable-org tables, no `::uuid` cast for text-org `org_counters`) on all 191 tenant tables; `rlsTenant` middleware (`lib/trpc.ts`, innermost of `protectedProcedure`) wraps each authed request in a tx that `SET LOCAL ROLE app_runtime` + `set_config('app.org_id', …, true)` and swaps `ctx.db` to the tx handle (both tx-local, auto-reset on commit/rollback, reuses the pooled superuser conn with no role leak; seeds/workers/migrator/owner stay unconstrained). **Correctness catch:** the app DB user is a superuser + BYPASSRLS, so `FORCE` alone is silently bypassed — the `SET LOCAL ROLE` drop is what makes RLS actually enforce (also true in prod, which runs as superuser `coheronconnect`). · `rls-tenant-isolation.test.ts` (8 DB-level cases: role is non-super/non-bypass, raw un-filtered SELECT sees only own org, cross-org INSERT/UPDATE/DELETE blocked, platform-default `org_id IS NULL` rows visible, owner pool unconstrained, no role leak post-tx).
- 2026-07-20 · **G1–G17 gap-closure run complete** (migs `0041`–`0052`). Follow-up hygiene: `crm/lead-scoring.ts` `rescore` gained explicit `.input(z.object({}))`; `india-compliance.ts` `slabsJson/payloadJson/ackJson` typed `.$type<Record<string, unknown>>()` (open-blob guards green).
- 2026-07-21 · G8 attendance→payroll LOP concluded end-to-end · **leave→attendance reflex** (`lib/india/leave-attendance.ts`, wired into `hr.ts` `leave.approve` tx: unpaid→`absent`/LOP, paid→`on_leave`/zero-LOP, upsert over prior rows) + `bulkMark` converted to upsert (corrections stick) + **external daily-attendance ingest** (`lib/india/attendance-ingest.ts` normaliser: derives status/hours/late/overtime, last-write-wins dedupe; `attendance.ingest` router resolves human `employeeCode`→UUID org-scoped, skips+reports unknown codes, idempotent upsert on the unique index) + **employee self-service capture** (`attendance.{signIn,signOut,myToday}`: `protectedProcedure` resolving emp from `ctx.user`, one row/day first-in/last-out, no-employee guard — the first-party HRMS punch path). No migration (`attendance_records` already present). · `attendance-self-service.test.ts` (7) + `leave-attendance-expand.test.ts` (7) + `leave-attendance-reflex.test.ts` (8) + `attendance-ingest-normalise.test.ts` (10) + `attendance-ingest-router.test.ts` (5) + `attendance-lop.test.ts` (4) = 41 cases.
- 2026-07-21 · **G2 fully closed** — ESIC monthly-return + state PT-challan portal push (completing PF/ESI/PT after the ECR loop) · `services/integrations/esic-return.ts` (`/v1/mc/upload`, 17-digit employer-code) + `services/integrations/pt-challan.ts` (`/v1/pt/challan`, per-state) GSP adapters (both registered) · `workflows/esiReturnWorkflow.ts` (`coheronconnect-esi-return`) + `workflows/ptChallanWorkflow.ts` (`coheronconnect-pt-challan`) — BullMQ 5-attempt backoff, idempotent on challan number, `not_configured` soft-fail, `failed`+rethrow, wired into `services/workflow.ts` · `india-compliance.filing.{submitEsi,statusEsi,submitPt,statusPt}` rebuild the MC lines (0.75%/3.25%) / PT lines (`professionalTax>0`) from the payroll run + payslips · `esi_challan_records` + `pt_challan_records` tables + `statutory_return_status` enum (mig `0055_lean_centennial`; snapshot chain repaired — backfilled the missing `0054_snapshot.json` so `0055` diffs cleanly; validated 0000→0055 on a throwaway DB with zero drift) · `esi-return.test.ts` (5) + `pt-challan.test.ts` (5) = 10 cases. Guard follow-ups: `connector-adapters` EXPECTED list gained `esic_return`/`pt_challan`; `hr.ts` `resolveSelfEmployeeWithShift` `db` param retyped `DbOrTx` (no-any-ratchet).
- 2026-07-21 · G8 shift-schedule-aware self-service punch · `shift_schedules` table (start/duration/grace minutes-from-midnight, one-default-per-org partial unique index) + `employees.shiftScheduleId` FK (set null), mig `0053_redundant_mojo` (validated on throwaway DB) · pure `lib/india/shift-schedule.ts` (`resolveShift` precedence assigned→org-default→built-in, `derivePunch` present/late/half_day + late/overtime/hours, all clamped ≥0) · `hr.signIn`/`hr.signOut` derive status against the effective shift · admin CRUD `hr.shifts.{list,create,update,setDefault,assign}` (create/setDefault demote the incumbent default in-tx; update/setDefault/assign org-scoped with NOT_FOUND on foreign ids) · `shift-schedule-resolve.test.ts` (14 unit) + `shift-schedule-router.test.ts` (11 e2e: CRUD, default demotion, assign link/unlink, cross-tenant refusal, late/present/default-driven derivation); existing self-service `present` test made shift-deterministic. G8 now 66 cases.
