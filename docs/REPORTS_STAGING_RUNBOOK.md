# Reports — staging runbook (Seq 8)

**Companion:** `docs/ITSM_STAGING_RUNBOOK.md`.

---

## Checklist

1. **`/app/reports`** loads with seeded ticket volume.
2. API `executiveOverview` / `slaDashboard` return within latency targets for typical org sizes (watch DB load).
3. Org **SLA calendar** settings present before relying on **`slaWhatIf`** in demos.

---

| Version | Date | Notes |
|---------|------|-------|
| 1.0 | 2026-04-07 | Seq 8 C3 |
