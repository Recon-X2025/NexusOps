# Handover — 2026-08-24 → next session

Paste this at the start of the next chat. It is the single source of truth for
where we are; verify every claim against the repo before acting (see "Verify
first").

---

## TL;DR

- **All 6 BLOCKERs + all 14 HIGH from the product-wide audit are fixed & tested.**
- **Live in production:** the 6 BLOCKERs + 7 HIGH (origin/main = `bed0c79`, deployed green).
- **Queued locally, NOT pushed:** 7 HIGH + 2 docs = **9 commits** ahead of origin/main.
- **One decision blocks the next push:** #15 (RBAC) — ship or hold (see below).
- **Owner-set priority for this session: DASHBOARD WIRING (H3–H8) first**, before product-wide MEDIUM/LOW.

## Verify first (don't trust this doc blindly)

```bash
cd /Users/kathikiyer/Documents/NexusOps
git log --oneline --reverse origin/main..main   # should be the 9 commits below
git log --oneline -1 origin/main                 # live HEAD = bed0c79
gh run list --branch main --limit 1              # last deploy: success
git status --short                               # clean
cat docs/PLAN-05.md                              # run 17 = full state; note PLAN-06 rollover due
```

## The 9 unpushed commits (oldest→newest)

```
1d0c323 fix(tax): professional tax OLD-regime only (#7)
f38640d fix(offboarding): revoke INVITED leavers (#19)
d9e2076 fix(invoice-pdf): statutory invoiceDate not createdAt (#18)
2f7b80f fix(gst): auto-enqueue e-invoice IRN on receivable creation (#17)
fe334fc fix(rbac): custom-role CRUD+manage → runtime RbacActions (#15) <-- REVIEW
40ac6b2 fix(form16): engine-sourced deductions, not hardcoded 50k (#9)
b6bedc8 fix(leave): year-end close must not lapse comp-off (#14)
b200e55 docs(plan): run 17
05de718 docs(plan): dashboard wiring priority
```

## DECISION NEEDED before pushing — #15 (RBAC)

`fe334fc` (`apps/api/src/lib/rbac-db.ts`) **widens live authorization**: custom-role
`create/update/manage` grants that were silently inert now take effect (mapped to
write/admin). Fail-safe (only honours explicit admin-set grants), but it changes
who-can-do-what in prod. **Ask the owner: ship it or drop it from the push.**
- Ship → push all 9 as one batch.
- Hold → `git rebase` or cherry-pick to exclude `fe334fc`, push the rest.

## Push discipline (when cleared)

1. Owner takes a **Vultr snapshot** (Claude cannot; a push to main deploys).
2. `gh run list --branch main --limit 1` — do NOT push while a run is in flight.
3. `pnpm build` (expect `Cached: <11`) **and** `pnpm lint:cold` (needs `Cached: 0`).
4. Ideally run the affected api tests (auth, hr/leave, accounting, procurement, financial, rbac) first.
5. `git push origin main` → watch the run reach the terminal **Deploy to Vultr** job.

## THIS SESSION'S PRIORITY — Dashboard wiring (do first)

Full report: `reports/audit-dashboard-wiring.md`. Already fixed earlier: the
aggregate-truncation BLOCKER, flow-metric posture (H2), dead drill links (H1).
**Still open — fix in this order of impact:**

- **H3** — "this period" health lights that are actually all-time totals (stuck red forever): `tickets.sla_compliance`, `coo.vendor_sla_breaches`, `financial.burn_rate`, `security.incidents_open_total` (in `packages/metrics/src/contributions/*`). Window them to the period.
- **H4** — no sample-size floor: one row lights a confident "stressed" CEO alert (e.g. `csm.churn_rate_30d`). Add a minimum-N gate.
- **H5** — fabricated operational claims: PMO "status report overdue" (`services/workbench-payloads/pmo.ts`), service-desk on-shift roster = `members[0]` + `endsAt:null` (`service-desk.ts`).
- **H6** — both devops metrics `appearsIn: []` (`contributions/devops.ts`) → deploy-failure alert can never fire. Give them an `appearsIn`.
- **H7** — Command Center serves finance figures (cash runway/burn/margin) to roles with `command_center:read` but no `financial:read` (`routers/command-center.ts` + payload). Owner policy call, then filter finance metrics or trim grants.
- **H8** — web failure/empty states read as "all clear": disabled query → infinite skeleton; workbench fetch error → "No data yet"; requester sees hub links then AccessDenied (`apps/web/src/app/app/command/page.tsx`, `components/dashboard/hub-command-center-page.tsx`, `components/workbench/**`).

Each: verify in source → fix → test (real Postgres on **5433**, `pnpm docker:test:up`) → `pnpm build` → commit. Batch-push at the end with owner's go.

## Test debt (fix + typecheck done; tests deferred)

- **#10** `journal.reverse` and **#11** lead-convert (`lib/crm/lead-convert.ts`) — the FOR UPDATE fix is in and typechecks, but their dedicated concurrency tests are missing. Add gated-race tests modeled on `apps/api/src/__tests__/concurrency-cluster.test.ts` (copy its `gateReadThenWrite` helper).

## Remaining backlog (after dashboard)

- Product-wide register: **12 MEDIUM + 6 LOW** — `reports/audit-product-wide.md`.
- **Form 16 Part A via TRACES** — roadmap. Part B (employer computation) is done and correct; Part A is the TRACES download flow (`tdsChallanRecords`), still unbuilt. Code flags it P2 (`services/form16-pdf.ts`).

## Housekeeping

- **PLAN-06 rollover is due** — `docs/PLAN-05.md` is over the 500-line cap. Create
  `docs/PLAN-06.md`, carry CURRENT STATE + THE QUEUE forward, leave the RUN LOG in
  05, put a pointer at the top of 05. (Flagged at the top of 05's run log.)

## Repo rules that bit us / must be followed

- Tests run against **real Postgres, port 5433** (`pnpm docker:test:up`); dev DB is 5434. Never run vitest concurrently with E2E.
- `packages/metrics` and `packages/payroll-math` are consumed via compiled `dist/` — **rebuild after editing** (`pnpm build`), or the api sees stale code.
- Import order in tests: helpers → router → `@coheronconnect/db` (avoids a circular-init crash).
- Money-path/statutory changes: correct any **blessing test** in the same pass (a test that asserted the buggy value). Seen with #7 (Form-16 108,390→108,750).
- CLAUDE.md is the operating contract; `docs/PLAN-*.md` (latest) is state.
